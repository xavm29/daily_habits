import 'dart:core';
import 'dart:core';
import 'package:daily_habits/models/goals_model.dart';

class ReminderGoal {
  int repeatDaily = 0;
  if (DateTime.now() < endDate) {
    repeatDaily =kDaily +1 }

}